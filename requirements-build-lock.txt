# Market AI Windows build-tool lock
# Python 3.13 / Windows x64 baseline used by build-market-ai.ps1.
# Installed with --no-deps in the same isolated temporary build venv.
# pip itself is pinned so the wheel/sdist installer does not drift between build PCs.
pip==25.1.1
PyInstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
altgraph==0.17.4
packaging==25.0
pefile==2024.8.26
pywin32-ctypes==0.2.3
setuptools==82.0.1
wheel==0.45.1
